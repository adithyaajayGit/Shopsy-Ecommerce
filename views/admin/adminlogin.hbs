<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Form</title>
  <style>
    /* Adjust margins and paddings for better alignment */
    .form-group {
      margin-bottom: 20px;
    }

    /* Center the form */
    .form-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 92vh; /* Adjusted height */
    }

    /* Set max-width to prevent the form from stretching too wide */
    .form-container form {
      max-width: 500px; /* Adjusted width */
      width: 100%;
      padding: 20px;
      border-radius: 10px;
      background-color: #f8f9fa; /* Light gray background color */
      box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1); /* Subtle box shadow */
    }

    /* Style form inputs */
    .form-control {
      border-radius: 8px;
      padding: 12px;
      border: 1px solid #ced4da; /* Light gray border */
      width: 100%;
      box-sizing: border-box;
    }

    /* Style submit button */
    .btn-primary {
      border-radius: 8px;
      padding: 12px 24px;
      background-color: #007bff; /* Blue color */
      border: none;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    /* Change button color on hover */
    .btn-primary:hover {
      background-color: #0056b3; /* Darker blue color */
    }

    /* Error message */
    .error {
      color: #dc3545; /* Red color */
      margin-top: -10px;
      margin-bottom: 10px;
    }

    /* Additional Styles */
    label {
      font-weight: bold;
      margin-bottom: 5px;
    }

    /* Image container */
    .image-container {
      text-align: center;
      margin-bottom: 20px;
    }

    /* Image styling */
    .login-image {
      max-width: 50%;
      height: auto;
    }

    /* Account registration link */
    .registration-link {
      text-align: center;
      margin-top: 20px;
    }
  </style>
</head>
<body>

<div class="form-container">
  <form action="/adminlogin" method="post">
    {{#if loginErr}}
    <p class="error">{{loginErr}}</p>
    {{/if}}
    <div class="image-container">
      <img src="https://serve2business.com/wp-content/uploads/2021/09/unnamed.png" alt="Login Image" class="login-image"> <!-- Placeholder image, replace with your image source -->
    </div>
    <div class="form-group">
      <label for="exampleInputEmail1">Email address</label>
      <input type="email" class="form-control" id="exampleInputEmail1" name="Email" aria-describedby="emailHelp" placeholder="Enter email">
    </div>
    <div class="form-group">
      <label for="exampleInputPassword1">Password</label>
      <input type="password" class="form-control" id="exampleInputPassword1" name="Password" placeholder="Password">
    </div>
    
    <button type="submit" class="btn btn-primary">Login</button>
    <div class="registration-link">
      Don't have an account? <a  href="/adminsignup"> Register here</a>
    </div>
  </form>
</div>

</body>
</html>
